apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: staging
  name: teamy-backend
  labels:
    app: teamy
    tier: backend
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: short
      tier: backend
  template:
    metadata:
      labels:
        app: short
        tier: backend
    spec:
      containers:
        - name: short-backend-staging
          image: byliuyang/teamy-backend-staging
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          args:
            - ./build/app
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  namespace: staging
  name: short-backend
spec:
  selector:
    app: short
    tier: backend
  ports:
    - name: graphql
      port: 8080

---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  namespace: staging
  name: short-backend
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/frontend-entry-points: http,https
    traefik.ingress.kubernetes.io/redirect-entry-point: https
    traefik.ingress.kubernetes.io/redirect-permanent: "true"
    traefik.ingress.kubernetes.io/whitelist-source-range: "0.0.0.0/0"
    ingress.kubernetes.io/whitelist-x-forwarded-for: "true"
spec:
  tls:
    - hosts:
        - gql-staging.short-d.com
      secretName: short-d.com-tls
  rules:
    - host: gql-staging.short-d.com
      http:
        paths:
          - backend:
              serviceName: short-backend
              servicePort: graphql